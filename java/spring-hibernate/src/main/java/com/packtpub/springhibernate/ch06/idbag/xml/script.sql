DROP TABLE STUDENT;
DROP TABLE STUDENT_PAPER;

CREATE TABLE STUDENT (
  ID BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1),
  FIRST_NAME VARCHAR(50),
  PRIMARY KEY(ID)
);

CREATE TABLE STUDENT_PAPER (
  STUDENT_PAPER_ID BIGINT GENERATED BY DEFAULT AS IDENTITY(START WITH 1),
  STUDENT_ID BIGINT,
  PAPER_PATH VARCHAR(50)
);

INSERT INTO STUDENT (FIRST_NAME) VALUES
('John'),
('Robert'),
('Kevin');

INSERT INTO STUDENT_PAPER (STUDENT_ID, PAPER_PATH) VALUES
(1, 'Paper1.doc'),
(2, 'Paper2.html'),
(2, 'Paper3.doc');